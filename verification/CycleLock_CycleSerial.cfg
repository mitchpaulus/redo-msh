\* ONE worker over the cyclic graph a <-> b: today's serial traversal.
\* The chain check must catch the cycle and fail cleanly (no deadlock).
SPECIFICATION Spec
CONSTANT Targets <- CycleTargets
CONSTANT DepsOf <- CycleDeps
CONSTANT Workers <- OneWorker
CONSTANT StartOf <- StartA
INVARIANTS TypeOK BuildOnce LockOwnedByBusy
PROPERTIES EventuallyHalted
