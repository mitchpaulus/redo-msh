\* Fan-out of three (a -> {b,c,d} -> e) with J=2: more parallel work than
\* tokens, shared leaf dep. Exercises the token queueing paths.
SPECIFICATION Spec
CONSTANTS
    J = 2
    BuildsCanFail = TRUE
CONSTANT Targets <- WideTargets
CONSTANT DepsOf <- WideDeps
CONSTANT Roots <- RootsA
INVARIANTS TypeOK BuildOnce TokenBound DepsSettledFirst
PROPERTIES EventuallyQuiescent Settles
