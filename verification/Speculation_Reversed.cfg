\* DOCUMENTED GAP (kept failing on purpose, like CycleLock_CycleParallel):
\* one dependency edge reversed between runs — recorded a -> b, actual
\* b -> a, real graph acyclic. BWait's severity rule hard-fails b through
\* a's SPECULATIVE cedge in 4 states, violating NoFailure and refuting
\* "stale edges can waste work but never invent an error" for this design.
\* run.sh expects the violation; the corrected machine (SpeculationMP.tla,
\* SpeculationMP_Reversed.cfg) passes the same shape.
SPECIFICATION Spec
CONSTANTS
    Targets <- TwoTargets
    Recorded <- RevRecorded
    Actual <- RevActual
    Roots <- RootsAB
    Cleanable <- TwoTargets
    J = 2
    BuildsCanFail = FALSE
INVARIANTS
    TypeOK
    NoFailure
