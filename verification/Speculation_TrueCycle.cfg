\* THE FRESH-DATABASE CYCLE CHECK. No recorded edges (nothing to
\* speculate on), but the do-files genuinely form a cycle a <-> b,
\* discovered only as they run -- today's purest deadlock scenario
\* (empty database, cyclic project, two concurrent demands). Mid-build
\* wait edges must close the cycle in the waits-for graph and fail HARD:
\* every interleaving ends with both targets failed (AllFail), never a
\* deadlock. Cleanable is empty (a never-built target cannot verify).
SPECIFICATION Spec
CONSTANTS
    J = 2
    BuildsCanFail = FALSE
CONSTANT Targets <- TwoTargets
CONSTANT Recorded <- NoEdges2
CONSTANT Actual <- CycleEdges
CONSTANT Roots <- RootsAB
CONSTANT Cleanable <- NoTargets
INVARIANTS TypeOK BuildOnce TokenBound ActualDepsFirst VerifiedSoundly
PROPERTIES EventuallyQuiescent AllFail
