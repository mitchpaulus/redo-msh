\* Same diamond with a single token: maximum token starvation. The
\* traversal must still settle everything (checking is never token-bound).
SPECIFICATION Spec
CONSTANTS
    J = 1
    BuildsCanFail = TRUE
CONSTANT Targets <- DiamondTargets
CONSTANT DepsOf <- DiamondDeps
CONSTANT Roots <- RootsA
INVARIANTS TypeOK BuildOnce TokenBound DepsSettledFirst
PROPERTIES EventuallyQuiescent Settles
